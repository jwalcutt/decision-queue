#!/bin/bash -e

# A stale pidfile from an unclean shutdown stops Puma from starting.
rm -f tmp/pids/server.pid

# Creates the database on first boot (then seeds), migrates on later boots.
bin/rails db:prepare

exec "$@"
